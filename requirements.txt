# Gradio web app (first -- pip aborts on later failures before reaching this)
gradio>=4.44.0

# ASGI server + web framework (entrypoint: uvicorn server:app)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Databricks SDK (WorkspaceClient for AI endpoint OAuth auth)
databricks-sdk>=0.30.0

# EDIFACT engine runtime
pandas>=2.0.0
openpyxl>=3.1.0
python-dateutil>=2.8.0
paramiko>=3.5.0
PyYAML>=6.0.1
Pillow>=11.0.0
pdfplumber>=0.11.0

# HTTP client for AI calls
requests>=2.31.0

PyMuPDF>=1.23.0

python-multipart>=0.0.9
pydantic>=2.0.0