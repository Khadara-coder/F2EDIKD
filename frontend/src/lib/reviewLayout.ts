/** Shared layout constants for the revue PDF / lines split panel. */
export const REVIEW_LINES_PAGE_SIZE = 10;

/** Min height of the main scrollable area so PDF and lines panels align. */
export const REVIEW_PANEL_BODY_MIN_HEIGHT = 640;
