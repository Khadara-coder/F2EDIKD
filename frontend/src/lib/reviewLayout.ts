/** Shared layout constants for the revue PDF / lines split panel. */
export const REVIEW_LINES_PAGE_SIZE = 10;

/** Desktop min height so PDF and lines panels align. */
export const REVIEW_PANEL_BODY_MIN_HEIGHT = 520;

/** Mobile / tablet: shorter panel to avoid fighting the page scroll. */
export const REVIEW_PANEL_BODY_MIN_HEIGHT_MOBILE = 280;
